# DSPy Workshop for Disruptive London Aug 13, 2024

To begin, create virtual environment and install dependencies

Mac:

```shell
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Open `dspy-intro.ipynb`
